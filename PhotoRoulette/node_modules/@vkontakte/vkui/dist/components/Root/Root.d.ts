import * as React from 'react';
import { type NavIdProps } from '../../lib/getNavId';
import type { HTMLAttributesWithRootRef } from '../../types';
export interface RootProps extends HTMLAttributesWithRootRef<HTMLDivElement>, NavIdProps {
    activeView: string;
    onTransition?: (params: {
        isBack: boolean;
        from: string;
        to: string;
    }) => void;
    children: React.ReactElement | Iterable<React.ReactElement>;
}
export interface RootState {
    activeView: string;
    transition: boolean;
    isBack?: boolean;
    prevView?: string;
}
/**
 * @see https://vkcom.github.io/VKUI/#/Root
 */
export declare const Root: ({ children, activeView: _activeView, onTransition, nav, ...restProps }: RootProps) => React.ReactNode;
//# sourceMappingURL=Root.d.ts.map